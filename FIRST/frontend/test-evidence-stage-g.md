# G — account management frontend evidence

Task: `g-web`, FIRST only. Implementer owns `FIRST/frontend/**`; backend F gate already accepted by parent. Independent H review/verification is still required; this record is implementation evidence only.

Files: `src/components/account-form.tsx`, `account-status.tsx`, `recovery-forms.tsx`, `auth-form.tsx`; app home/account/recovery/verification routes, layout/CSS; `src/lib/backend-proxy.ts`; `tests/account.test.tsx`, `recovery-routes.test.tsx`, `forms.test.tsx`; frontend README.

Implemented: real same-origin API profile PATCH; email-change request preserving existing address; reauthentication challenge and explicit operation retry; password change/reset; verification confirmation POST only; resend and expired-link recovery; sessions list/individual DELETE/all POST; blank home session/navigation/reminder; login/register to home, logout/revocation/password change to login. All mutation forms use fresh CSRF, pending locks, accessible errors, and retryable failures. Email verification refreshes me and handles logged-out email-change confirmation. Unicode username NFC; password length remains backend codepoint-validated rather than HTML UTF16 limits. Proxy timeout30s preserves signed IP transport.

## Checks and retained attempts

All successful npm commands run from `FIRST/frontend`. Test mocks are confined to tests; no listening service, Docker, SMTP or database service started.

| Command | Result / evidence | Not performed |
|---|---|---|
| `npm test` mistakenly at repository root | FAIL exit254, ENOENT package.json; `/tmp/first-g-test.log`. Corrected working directory. | No tests executed by failed command |
| `npm test` in frontend | PASS57 tests/4files; `/tmp/first-g-test2.log` | Runtime integrations excluded |
| `npm run lint` | PASS; `/tmp/first-g-lint.log` | Browser layout excluded |
| `npm test` after Unicode/route tests | PASS61 tests/5files; `/tmp/first-g-test3.log` | Runtime integrations excluded |
| `npm run typecheck` concurrently with build | FAIL TS6053: .next/types route files were regenerated by Next build; `/tmp/first-g-typecheck.log`. Corrective action: run sequentially. | No successful typecheck claimed for this attempt |
| `npm run build` | PASS, Next16.3.5 generated all9 routes; `/tmp/first-g-build.log` | Build starts no app listener and does not contact backend |
| `npm run typecheck` mistakenly at repository root | FAIL exit254, ENOENT package.json; `/tmp/first-g-typecheck2.log`. Corrected working directory. | No typecheck executed |
| `npm run typecheck` standalone | PASS; `/tmp/first-g-typecheck3.log` | Real browser excluded |
| `npm test` final source | PASS61 tests/5files; `/tmp/first-g-test4.log` | Runtime integrations excluded |
| `npm run lint` final source | PASS; `/tmp/first-g-lint2.log` | Real browser excluded |
| `npm run build` final source | See final build result below; `/tmp/first-g-build2.log` | Runtime/deployment excluded |

Coverage: existing35 E tests retained/updated for home redirect and completed account target;26 additional tests cover profile request shape/NFC/CSRF; email old-address preservation; reauth challenge + retained fields + explicit retry;401/403/429/503; field accessibility; pending duplicate suppression; password change; generic reset request; link GET/mount no requests; verification success with authenticated/anonymous me; invalid token recovery/resend; reset credentials/invalid/missing tokens; individual/current/all session revocation; session401 retry; route parameter validation; Unicode20-codepoint passwords for register and reset.

`not_verified`: actual PostgreSQL, Redis atomicity/TTL/outage, SMTP delivery/timing, trusted proxy ingress and signed-IP clocks, deployed HTTPS/cookies/CSRF, browser E2E/responsiveness/keyboard use, remote deployment. Component DOM assertions and static build are not live service verification. No independent review is claimed here. Parent owns run/checklist/docs outside frontend and commit/push.

Final build result: PASS exit0; webpack compile, built-in TypeScript, static generation9/9 completed. Routes `/`, `/giris`, `/kayit`, `/hesap`, `/sifremi-unuttum` static; token pages and auth proxy dynamic. Source freeze after these results; H reviewers may request revisions.
