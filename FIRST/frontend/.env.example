# Server-only, fixed Django origin. Never prefix with NEXT_PUBLIC_.
BACKEND_URL=https://first-backend.example.invalid
# Optional per-client IP limiting; both variables or neither. Secret >=32 chars.
# AUTH_PROXY_SECRET=
# AUTH_CLIENT_IP_HEADER=x-trusted-client-ip
# Trusted ingress MUST overwrite that header and block direct untrusted access.
