# H documentation and artifact hygiene

Task h-delivery; manager owns FIRST README/auth-kararlari/teknik-kararlar/contracts/auth-api, frontend README and run records. Canonical contract now describes implemented normal auth; OAuth remains future. Backend/frontend env examples verified placeholders and server-only secrets.

Independent /root/contract_audit read-only documentation follow-up: contract endpoint/field/expiry/session/CSRF/proxy/error facts aligned. One documentation check FAILED: frontend README claimed Node20.9+ for the full project, but Vite/jsdom/Vitest development tool engines require narrower supported versions. Inspection command `node -e` read installed package.json engines. Correction: README now specifies Node20.19+ within20.x,22.13+ within22.x,or24+; tested25.8.2 retained. No runtime source changed; broad tests were not rerun for this text-only correction.

Manager checks: `git diff --check` passed; `git check-ignore FIRST/backend/.venv FIRST/frontend/node_modules FIRST/frontend/.next FIRST/backend/.env FIRST/backend/test.sqlite3` confirmed all ignored; `git ls-files FIRST | rg '(^|/)(\.env$|\.venv/|node_modules/|\.next/|__pycache__/|.*\.sqlite3$)'` returned no matches (rg exit1 expected, no leaked tracked artifacts). `node .orchestrator/bin/orchestrator.mjs validate .orchestrator/runs/first-auth/run.json` valid with no errors/warnings, history valid. Checklist generated directly from graph; failed attempts are visibly historical and reference passed correction gates.

Not performed by design: real PostgreSQL migrations/constraints/row locks/concurrency; Redis Lua atomicity/TTL/eviction/network failure; SMTP delivery/timing and partial external side effects; broad external password breach corpus validation; trusted ingress/clock sync/proxy/HTTPS cookies; browser E2E/visual/responsive/keyboard manual use; Docker/listening runtime/remote deployment. All not_verified; no such service started or live email sent.

Manual environment follow-up: provision real secrets and reviewed breach corpus; apply migrations on isolated target environment; validate ingress/IP/HMAC and Secure cookie/CSRF; exercise all auth forms and token lifetimes/session invalidation against real PostgreSQL/Redis/mail; browser accessibility/responsive checks and deployment verification. These do not block this explicitly code-only delivery.

Manager recheck attempts retained: initial `node -e` using require(package+'/package.json') failed exit1 at plugin-react due package export restrictions after printing Vite. Corrected command used fs.readFileSync('node_modules/'+package+'/package.json') and JSON.parse; exit0 verified Vite/plugin-react ^20.19.0||>=22.12.0, jsdom ^20.19.0||^22.13.0||>=24.0.0, Vitest ^20.0.0||^22.0.0||>=24.0.0. Revised README meets intersection. This was an inspection-command failure, not product test failure.
